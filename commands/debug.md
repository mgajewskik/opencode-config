---
description: Safe, evidence-led debugging one probe at a time
---

First load the `debug` skill using the `skill` tool. Then use it in the current primary agent to diagnose this problem: $ARGUMENTS

Do not select another agent or model. If no problem is provided, follow the skill's intake process and ask only for the missing safety-critical context.
