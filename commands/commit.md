---
description: Create a proper Git commit from current changes
---

Use the `commit` skill to handle this request.

Treat `$ARGUMENTS` as optional framing hints. Let the skill own candidate selection, staging, split detection, message construction, literal preview, execution within existing authority, and result reporting.
